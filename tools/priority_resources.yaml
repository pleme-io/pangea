# Priority AWS Resources for Implementation
# Based on common usage patterns and architectural needs

resources:
  # Core Networking (Already implemented: vpc, subnet, security_group, internet_gateway, route_table, nat_gateway)
  - aws_eip                           # Elastic IP addresses
  - aws_eip_association               # Associate EIP to instances
  - aws_network_acl                   # Network ACLs
  - aws_network_acl_rule              # NACL rules
  - aws_route                         # Individual routes
  - aws_route_table_association       # Associate route tables to subnets
  - aws_vpc_endpoint                  # VPC endpoints for AWS services
  
  # Storage (Already implemented: s3_bucket)
  - aws_s3_bucket_policy              # Bucket policies
  - aws_s3_bucket_acl                 # Bucket ACLs
  - aws_s3_bucket_versioning          # Enable versioning
  - aws_s3_bucket_lifecycle_configuration # Lifecycle rules
  - aws_s3_object                     # S3 objects
  - aws_ebs_volume                    # EBS volumes
  - aws_volume_attachment             # Attach EBS to instances
  
  # Compute (Already implemented: instance, launch_template, autoscaling_group)
  - aws_ami                           # AMI management
  - aws_key_pair                      # SSH key pairs
  - aws_eip                           # Elastic IPs
  - aws_placement_group               # Placement strategies
  
  # Load Balancing (Already implemented: lb, lb_target_group, autoscaling_attachment)
  - aws_lb_listener                   # Load balancer listeners
  - aws_lb_listener_rule              # Listener routing rules
  - aws_lb_target_group_attachment    # Attach targets to groups
  
  # Database (Already implemented: db_instance)
  - aws_db_subnet_group               # DB subnet groups
  - aws_db_parameter_group            # DB parameters
  - aws_rds_cluster                   # Aurora clusters
  - aws_rds_cluster_instance          # Aurora instances
  - aws_elasticache_subnet_group      # Cache subnet groups
  - aws_elasticache_cluster           # ElastiCache clusters
  - aws_elasticache_replication_group # Redis replication
  - aws_dynamodb_table                # DynamoDB tables
  
  # Security & IAM (Already implemented: iam_role)
  - aws_iam_policy                    # IAM policies
  - aws_iam_user                      # IAM users
  - aws_iam_group                     # IAM groups
  - aws_iam_instance_profile          # Instance profiles
  - aws_iam_role_policy_attachment    # Attach policies to roles
  - aws_iam_user_policy_attachment    # Attach policies to users
  - aws_kms_key                       # KMS encryption keys
  - aws_kms_alias                     # KMS key aliases
  
  # Monitoring (Already implemented: cloudwatch_metric_alarm)
  - aws_cloudwatch_log_group          # CloudWatch log groups
  - aws_cloudwatch_dashboard          # CloudWatch dashboards
  - aws_sns_topic                     # SNS topics
  - aws_sns_topic_subscription        # SNS subscriptions
  - aws_sqs_queue                     # SQS queues
  
  # Container Services
  - aws_ecs_cluster                   # ECS clusters
  - aws_ecs_service                   # ECS services
  - aws_ecs_task_definition           # Task definitions
  - aws_ecr_repository                # Container registries
  - aws_eks_cluster                   # EKS clusters
  - aws_eks_node_group                # EKS node groups
  
  # Serverless
  - aws_lambda_function               # Lambda functions
  - aws_lambda_permission             # Lambda invocation permissions
  - aws_lambda_event_source_mapping   # Event sources
  - aws_api_gateway_rest_api          # REST APIs
  - aws_api_gateway_resource          # API resources
  - aws_api_gateway_method            # API methods
  - aws_api_gateway_integration       # API integrations
  - aws_api_gateway_deployment        # API deployments
  
  # CDN & DNS
  - aws_cloudfront_distribution       # CloudFront CDN
  - aws_route53_zone                  # Route53 hosted zones
  - aws_route53_record                # DNS records
  
  # File Storage
  - aws_efs_file_system               # EFS file systems
  - aws_efs_mount_target              # EFS mount targets
  
  # Secrets & Parameters
  - aws_secretsmanager_secret         # Secrets Manager
  - aws_secretsmanager_secret_version # Secret versions
  - aws_ssm_parameter                 # SSM Parameter Store

# Total Priority Resources: ~75
# These represent the most commonly used AWS resources that cover
# the majority of cloud infrastructure patterns